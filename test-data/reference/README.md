# Reference Data

Expected output values for integration tests. Each JSON file contains both
C++ ground truth (from RecastNavigation CLI) and current Rust output.

## How values were generated

**C++ ground truth** was generated by running `RecastCLI` from the
[RecastNavigation](https://github.com/recastnavigation/recastnavigation)
repository with default parameters:

```bash
RecastCLI build --input test-data/meshes/nav_test.obj --output /tmp/nav_test.bin
RecastCLI find-path --mesh /tmp/nav_test.bin --start 5,0,5 --end 20,0,15
```

**Rust output** was generated by running `RecastBuilder::build_mesh()` and
`NavMeshQuery` with the same config parameters.

## Parity status

The Rust port produces identical polygon and vertex counts to C++ for all
three test meshes. Multi-polygon pathfinding (`find_path` and
`find_straight_path`) works correctly.

| Mesh | C++ Polys | Rust Polys | Ratio | C++ Verts | Rust Verts |
|------|-----------|------------|-------|-----------|------------|
| nav_test | 537 | 537 | 1.0x | 1197 | 1197 |
| dungeon | 217 | 217 | 1.0x | 452 | 452 |
| bridge | 8 | 8 | 1.0x | 18 | 18 |

## Bugs fixed during port

1. Direction mismatch in `contour.rs` `mark_boundaries` (reduced from 10-12x)
2. CompactSpan `y` field: was `span.min` (bottom of solid), should be `span.max` (walkable surface)
3. CompactSpan `h` field: was solid height, should be air gap height
4. Non-walkable spans included in compact heightfield (C++ filters them)
5. Missing walkableHeight/walkableClimb checks in connection building
6. Wrong direction indices in distance field propagation
7. Wrong diagonal neighbor lookup in box blur (used non-existent diagonal connections)
8. Watershed overflow on `max_distance + 1` wrapping
9. `normal.y.abs()` marked ceilings/undersides as walkable (should be `normal.y >` only)
10. Missing `erode_walkable_area` call after compact heightfield (C++ calls `rcErodeWalkableArea`)
11. `erode_walkable_area` used linked-list 8-direction API instead of `con[4]` array
12. `expand_regions` ran only 1 iteration (`stack.retain` broke termination check)
13. `merge_small_regions` picked largest neighbor instead of smallest; missing overlap/border checks
14. `distance_pt_seg` returned actual distance instead of squared distance (contour simplification)

## Integration test strategy

Tests validate both Rust output (regression) and C++ output (parity).
Pathfinding tests verify multi-polygon traversal and straight path
waypoint generation against the C++ reference coordinates in each JSON
file.
